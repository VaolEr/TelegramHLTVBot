language: java
jdk:
  - openjdk11
before_install:
  - openssl aes-256-cbc -K $encrypted_442a81cafda4_key -iv $encrypted_442a81cafda4_iv -in application-botSettings.properties.enc -out src\\main\\resources\\application-botSettings.properties -d
# git strips the wrapper jar file so we have to force its download during the build
install:
  - mvn -N io.takari:maven:wrapper
  - ./mvnw install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
script:
  #- mvn spring-boot:run -Dspring.profiles.active=botSettings
  - ./mvnw clean install
##- ./mvnw heroku:deploy jib:build -P deploy-heroku #
